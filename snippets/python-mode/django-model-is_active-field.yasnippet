# -*- mode: snippet -*-
# key: is_active
# --
class $1ActiveManager(models.Manager):

    def get_query_set(self):
        return super($1ActiveManager, self).get_query_set().filter(is_active=True)

    is_active = models.BooleanField(default=True)

    objects = models.Manager()
    active = $1ActiveManager()
